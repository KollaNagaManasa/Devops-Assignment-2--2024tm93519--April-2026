apiVersion: apps/v1
kind: Deployment
metadata:
  name: aceest-a
  namespace: aceest
spec:
  replicas: 1
  selector:
    matchLabels:
      app: aceest
      version: a
  template:
    metadata:
      labels:
        app: aceest
        version: a
    spec:
      containers:
      - name: app
        image: kollanagamanasa/aceest-fitness:latest
        ports:
        - containerPort: 5000
