apiVersion: v1
kind: Service
metadata:
  name: aceest-service
  namespace: aceest
spec:
  type: NodePort
  selector:
    app: aceest
    version: blue   # switch to green
  ports:
  - port: 80
    targetPort: 5000
